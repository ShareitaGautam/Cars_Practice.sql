# SQL Practice - Cars Table

This repo contains a PostgreSQL practice script demonstrating:

- CREATE TABLE
- INSERT
- SELECT
- ALTER TABLE (ADD/DROP COLUMN)
- UPDATE
- DELETE
- DROP TABLE

Run the script in pgAdmin Query Tool inside a database named `practice`.
